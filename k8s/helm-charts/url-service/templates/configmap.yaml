apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "url-service.fullname" . }}-config
  labels:
    {{- include "url-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}