spring:
  config:
    activate:
      on-profile: docker

  datasource:
    url: jdbc:postgresql://postgres:5432/urlshortener?currentSchema=analytics_service
    username: postgres
    password: postgres

  flyway:
    schemas: analytics_service
    create-schemas: true

  data:
    redis:
      host: redis
      port: 6379

app:
  aws:
    sqs:
      click-events-queue: http://localstack:4566/000000000000/click-events
